export enum Age {
  PUPPY,
  YOUNG,
  ADULT,
  OLD,
}

export enum Size {
  small,
  medium,
  big,
}

export enum Energy {
  low,
  medium,
  high,
}

export enum Independence {
  low,
  medium,
  high,
}
